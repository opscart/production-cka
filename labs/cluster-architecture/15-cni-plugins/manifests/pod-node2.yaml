apiVersion: v1
kind: Pod
metadata:
  name: pod-node2
  namespace: lab15-cni
spec:
  nodeSelector:
    kubernetes.io/hostname: opscart-m02
  containers:
  - name: net-test
    image: busybox
    command: ["sleep", "3600"]
