apiVersion: v1
kind: Service
metadata:
  name: web-app-nodeport
  namespace: lab01-services
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
